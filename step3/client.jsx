import React from 'react';
import ReactDOM from 'react-dom/client';

import ResponseCheckHooks from './ResponseCheckHooks';

const root = ReactDOM.createRoot(document.querySelector('#root'));
root.render(<ResponseCheckHooks />);
